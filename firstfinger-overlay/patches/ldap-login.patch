diff --git a/api/user_login.go b/api/user_login.go
index 8ec646d..d2e429c 100644
--- a/api/user_login.go
+++ b/api/user_login.go
@@ -37,6 +37,15 @@ import (
 	xnet "github.com/minio/pkg/v3/net"
 )
 
+func isLDAPFallbackEnabled() bool {
+	switch strings.ToLower(strings.TrimSpace(env.Get("CONSOLE_LDAP_ENABLED", "on"))) {
+	case "0", "false", "no", "off", "disabled":
+		return false
+	default:
+		return true
+	}
+}
+
 func registerLoginHandlers(api *operations.ConsoleAPI) {
 	// GET login strategy
 	api.AuthLoginDetailHandler = authApi.LoginDetailHandlerFunc(func(params authApi.LoginDetailParams) middleware.Responder {
@@ -124,6 +133,20 @@ func getConsoleCredentials(accessKey, secretKey string, client *http.Client) (*C
 	}, nil
 }
 
+func getLDAPConsoleCredentials(accessKey, secretKey string, client *http.Client) (*ConsoleCredentials, error) {
+	creds, err := auth.GetCredentialsFromLDAP(client, getMinIOServer(), accessKey, secretKey)
+	if err != nil {
+		return nil, err
+	}
+	return &ConsoleCredentials{
+		ConsoleCredentials: creds,
+		AccountAccessKey:   accessKey,
+		CredContext: &credentials.CredContext{
+			Client: client,
+		},
+	}, nil
+}
+
 // getLoginResponse performs login() and serializes it to the handler's output
 func getLoginResponse(params authApi.LoginParams) (*models.LoginResponse, *CodedAPIError) {
 	ctx, cancel := context.WithCancel(params.HTTPRequest.Context())
@@ -137,30 +160,38 @@ func getLoginResponse(params authApi.LoginParams) (*models.LoginResponse, *Coded
 	clientIP := getClientIP(params.HTTPRequest)
 	client := GetConsoleHTTPClient(clientIP)
 
+	sf := &auth.SessionFeatures{}
+	if lr.Features != nil {
+		sf.HideMenu = lr.Features.HideMenu
+	}
+
 	var err error
-	var consoleCreds *ConsoleCredentials
+	var sessionID *string
 	// if we receive an STS we use that instead of the credentials
 	if lr.Sts != "" {
-		consoleCreds = &ConsoleCredentials{
+		consoleCreds := &ConsoleCredentials{
 			ConsoleCredentials: credentials.NewStaticV4(lr.AccessKey, lr.SecretKey, lr.Sts),
 			AccountAccessKey:   lr.AccessKey,
 			CredContext: &credentials.CredContext{
 				Client: client,
 			},
 		}
+		sessionID, err = login(consoleCreds, sf)
 	} else {
 		// prepare console credentials
-		consoleCreds, err = getConsoleCredentials(lr.AccessKey, lr.SecretKey, client)
+		consoleCreds, credErr := getConsoleCredentials(lr.AccessKey, lr.SecretKey, client)
+		err = credErr
 		if err != nil {
 			return nil, ErrorWithContext(ctx, err, ErrInvalidLogin)
 		}
+		sessionID, err = login(consoleCreds, sf)
+		if err != nil && !xnet.IsNetworkOrHostDown(err, true) && isLDAPFallbackEnabled() {
+			ldapCreds, ldapErr := getLDAPConsoleCredentials(lr.AccessKey, lr.SecretKey, client)
+			if ldapErr == nil {
+				sessionID, err = login(ldapCreds, sf)
+			}
+		}
 	}
-
-	sf := &auth.SessionFeatures{}
-	if lr.Features != nil {
-		sf.HideMenu = lr.Features.HideMenu
-	}
-	sessionID, err := login(consoleCreds, sf)
 	if err != nil {
 		if xnet.IsNetworkOrHostDown(err, true) {
 			return nil, ErrorWithContext(ctx, ErrNetworkError)
diff --git a/web-app/src/screens/Console/Account/Account.tsx b/web-app/src/screens/Console/Account/Account.tsx
index b9ac00c..617eae9 100644
--- a/web-app/src/screens/Console/Account/Account.tsx
+++ b/web-app/src/screens/Console/Account/Account.tsx
@@ -278,6 +278,19 @@ const Account = () => {
           </Grid>
 
           <Grid item xs={12}>
+            {userIDP && (
+              <Box
+                withBorders
+                sx={{
+                  marginBottom: 15,
+                  padding: 15,
+                  color: "#d1d5db",
+                }}
+              >
+                This page lists access keys for the current LDAP identity. To
+                review local users and their keys, use Identity &gt; Users.
+              </Box>
+            )}
             <DataTable
               itemActions={tableActions}
               entityName={"Access Keys"}
diff --git a/web-app/src/screens/Console/Console.tsx b/web-app/src/screens/Console/Console.tsx
index ba8bda8..ccc620c 100644
--- a/web-app/src/screens/Console/Console.tsx
+++ b/web-app/src/screens/Console/Console.tsx
@@ -135,10 +135,17 @@ const Console = () => {
 
   const [openSnackbar, setOpenSnackbar] = useState<boolean>(false);
 
-  const ldapIsEnabled = (features && features.includes("ldap-idp")) || false;
   const kmsIsEnabled = (features && features.includes("kms")) || false;
   const obOnly = !!features?.includes("object-browser-only");
 
+  const getDefaultConsoleRoute = (routes: IRouteRule[]) => {
+    const preferredRoute = routes.find(
+      (route) => route.path === IAM_PAGES.DASHBOARD,
+    );
+
+    return preferredRoute?.path || routes[0]?.path;
+  };
+
   useEffect(() => {
     dispatch({ type: "socket/OBConnect" });
   }, [dispatch]);
@@ -251,7 +258,6 @@ const Console = () => {
     {
       component: Users,
       path: IAM_PAGES.USERS,
-      fsHidden: ldapIsEnabled,
       customPermissionFnc: () =>
         hasPermission(CONSOLE_UI_RESOURCE, [IAM_SCOPES.ADMIN_LIST_USERS]) ||
         hasPermission(S3_ALL_RESOURCES, [IAM_SCOPES.ADMIN_CREATE_USER]),
@@ -259,7 +265,6 @@ const Console = () => {
     {
       component: Groups,
       path: IAM_PAGES.GROUPS,
-      fsHidden: ldapIsEnabled,
     },
     {
       component: AddGroupScreen,
@@ -491,7 +496,7 @@ const Console = () => {
                 element={
                   <Fragment>
                     {allowedRoutes.length > 0 ? (
-                      <Navigate to={allowedRoutes[0].path} />
+                      <Navigate to={getDefaultConsoleRoute(allowedRoutes)} />
                     ) : (
                       <Fragment />
                     )}
diff --git a/web-app/src/screens/Console/Policies/PolicyDetails.tsx b/web-app/src/screens/Console/Policies/PolicyDetails.tsx
index 66de9c2..fd9b83c 100644
--- a/web-app/src/screens/Console/Policies/PolicyDetails.tsx
+++ b/web-app/src/screens/Console/Policies/PolicyDetails.tsx
@@ -64,7 +64,6 @@ import {
   setHelpName,
   setSnackBarMessage,
 } from "../../../systemSlice";
-import { selFeatures } from "../consoleSlice";
 import { useAppDispatch } from "../../../store";
 import TooltipWrapper from "../Common/TooltipWrapper/TooltipWrapper";
 import PageHeaderWrapper from "../Common/PageHeaderWrapper/PageHeaderWrapper";
@@ -80,8 +79,6 @@ const PolicyDetails = () => {
   const navigate = useNavigate();
   const params = useParams();
 
-  const features = useSelector(selFeatures);
-
   const [policy, setPolicy] = useState<Policy | null>(null);
   const [policyStatements, setPolicyStatements] = useState<IAMStatement[]>([]);
   const [userList, setUserList] = useState<string[]>([]);
@@ -99,8 +96,6 @@ const PolicyDetails = () => {
   const [deleteOpen, setDeleteOpen] = useState<boolean>(false);
   const [selectedTab, setSelectedTab] = useState<string>("summary");
 
-  const ldapIsEnabled = (features && features.includes("ldap-idp")) || false;
-
   const displayGroups = hasPermission(
     CONSOLE_UI_RESOURCE,
     listGroupPermissions,
@@ -180,7 +175,7 @@ const PolicyDetails = () => {
   useEffect(() => {
     const loadUsersForPolicy = () => {
       if (loadingUsers) {
-        if (displayUsers && !ldapIsEnabled) {
+        if (displayUsers) {
           api.policies
             .listUsersForPolicy(policyName)
             .then((result) => {
@@ -199,7 +194,7 @@ const PolicyDetails = () => {
 
     const loadGroupsForPolicy = () => {
       if (loadingGroups) {
-        if (displayGroups && !ldapIsEnabled) {
+        if (displayGroups) {
           api.policies
             .listGroupsForPolicy(policyName)
             .then((result) => {
@@ -262,7 +257,6 @@ const PolicyDetails = () => {
     displayUsers,
     displayGroups,
     displayPolicy,
-    ldapIsEnabled,
     dispatch,
   ]);
 
@@ -421,7 +415,7 @@ const PolicyDetails = () => {
               {
                 tabConfig: {
                   label: "Users",
-                  disabled: !displayUsers || ldapIsEnabled,
+                  disabled: !displayUsers,
                   id: "users",
                 },
                 content: (
@@ -471,7 +465,7 @@ const PolicyDetails = () => {
               {
                 tabConfig: {
                   label: "Groups",
-                  disabled: !displayGroups || ldapIsEnabled,
+                  disabled: !displayGroups,
                   id: "groups",
                 },
                 content: (
diff --git a/web-app/src/screens/Console/valid-routes.tsx b/web-app/src/screens/Console/valid-routes.tsx
index bb384e1..350dc26 100644
--- a/web-app/src/screens/Console/valid-routes.tsx
+++ b/web-app/src/screens/Console/valid-routes.tsx
@@ -90,7 +90,6 @@ export const validRoutes = (
   features: string[] | null | undefined,
   licenseNotification: boolean = false,
 ) => {
-  const ldapIsEnabled = (features && features.includes("ldap-idp")) || false;
   const kmsIsEnabled = (features && features.includes("kms")) || false;
 
   let consoleMenus: IMenuItem[] = [
@@ -147,14 +146,12 @@ export const validRoutes = (
             hasPermission(CONSOLE_UI_RESOURCE, [IAM_SCOPES.ADMIN_ALL_ACTIONS]),
           name: "Users",
           icon: <UsersMenuIcon />,
-          fsHidden: ldapIsEnabled,
         },
         {
           id: "groups",
           path: IAM_PAGES.GROUPS,
           name: "Groups",
           icon: <GroupsMenuIcon />,
-          fsHidden: ldapIsEnabled,
         },
         {
           name: "OpenID",
diff --git a/web-app/src/screens/LoginPage/Login.tsx b/web-app/src/screens/LoginPage/Login.tsx
index 8a2623d..4bb9992 100644
--- a/web-app/src/screens/LoginPage/Login.tsx
+++ b/web-app/src/screens/LoginPage/Login.tsx
@@ -28,9 +28,10 @@ import { getLogoApplicationVariant, getLogoVar } from "../../config";
 import { RedirectRule } from "api/consoleApi";
 import { redirectRules } from "./login.utils";
 import { setHelpName } from "../../systemSlice";
+import { IAM_PAGES } from "../../common/SecureComponent/permissions";
 
 export const getTargetPath = () => {
-  let targetPath = "/browser";
+  let targetPath = IAM_PAGES.DASHBOARD;
   if (
     localStorage.getItem("redirect-path") &&
     localStorage.getItem("redirect-path") !== ""
